apply plugin: "war"

bootWar {
    mainClassName = "com.nunsys.growthpath.GrowthpathApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
